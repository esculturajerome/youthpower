import React from "react";

import Login from "../components/login";

const LoginScreen = () => {
  return (
    <React.Fragment>
      <Login />
    </React.Fragment>
  );
};

export default LoginScreen;
